# Apple Support AI — Python dependencies
# Phases 1–3 scope: data pipeline + EDA + baseline classifiers + embedding
# classifier. Dependencies for later phases (faiss-cpu, streamlit, ...) will be
# added in the phase that actually uses them — this file only lists packages
# that are used *now* (assignment rule: "Add only packages that are actually
# used").

pandas>=2.2
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13
python-dotenv>=1.0
scikit-learn>=1.5
sentence-transformers>=3.0        # Phase 3: all-MiniLM-L6-v2 encoder (installs torch)
